fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.30
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.x reads bcrypt's legacy ``__about__`` metadata. bcrypt 4.1+
# removed it, which makes every password verification print a traceback even
# though authentication succeeds. 4.0.1 is the last compatible release.
bcrypt==4.0.1
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1

# OCR (requires the Tesseract binary installed separately at the OS level —
# see app/ai/ocr.py docstring. Uploads still work without it.)
pytesseract==0.3.10
Pillow==10.3.0

# Classical computer-vision pipeline for satellite/aerial image analysis
# (K-means color clustering + contour extraction — see app/ai/satellite_cv.py
# for exactly what this does and doesn't do vs. a trained deep-learning model)
numpy==1.26.4
opencv-python-headless==4.10.0.84
scikit-learn==1.5.0
