# Create a browser key in your own Google Cloud project. The public Google
# sample key must not be used here: it is restricted to Google's demo origins.
VITE_GOOGLE_MAPS_API_KEY=
# Optional: required only when using Advanced Markers or a Cloud-styled map.
VITE_GOOGLE_MAPS_MAP_ID=
